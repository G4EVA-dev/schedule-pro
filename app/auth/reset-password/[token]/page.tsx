"use client";

// Reuse the main reset password page component, which already handles the token via useParams/useSearchParams.
import ResetPasswordPage from "../page";

export default ResetPasswordPage;
